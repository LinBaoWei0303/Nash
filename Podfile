# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Nash' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Nash
  pod 'Alamofire', '~> 5.2'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'SQLite.swift', '~> 0.12.0'
  
  target 'NashTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NashUITests' do
    # Pods for testing
  end

end
